[user]
	name = Josh Cooley
	email = josh@cooley.tech
	signingkey = /Users/joombaga/.ssh/id_ed25519.pub
[merge]
	ff = only
[gpg]
	format = ssh
[push]
	autoSetupRemote = true
[commit]
	gpgsign = true
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
